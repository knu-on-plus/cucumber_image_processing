# Legacy research code

`raw_research_code/` is a snapshot of the previously tracked repository state.
It is retained as an implementation reference and is not the public pipeline
entrypoint.

Use the root-level `pipeline.py` and `occlusion_pipeline/` package for the
reproducible, staged implementation.
